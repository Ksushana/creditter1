/* eslint-disable no-new */
// import Tooltip from 'tooltip.js';

// const TOOLTIP_ATTR = 'data-tooltip';
// const BOUNDARIES_ATTR = 'data-tooltip-boundaries';

function init() {
  console.log('hhh')
  // const tooltipList = Array.from(
  //   document.querySelectorAll(`[${TOOLTIP_ATTR}]`),
  // );

  // if (Array.isArray(tooltipList) && tooltipList.length > 0) {
  //   tooltipList.forEach(tooltipElement => {
  //     const tooltipTitle = tooltipElement.getAttribute(TOOLTIP_ATTR);
  //     const tooltipBoundaries = document.querySelector(`[${BOUNDARIES_ATTR}]`);

  //     if (tooltipTitle) {
  //       new Tooltip(tooltipElement, {
  //         placement: 'top',
  //         title: tooltipTitle,
  //         boundariesElement: tooltipBoundaries || 'scrollParent',
  //       });
  //     }
  //   });
  // }
}

export default init;
